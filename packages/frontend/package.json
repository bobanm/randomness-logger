{
    "name": "randomness-logger-frontend",
    "version": "0.0.0",
    "description": "Frontend for the smart contract that uses Chainlink services.",
    "scripts": {
        "dev": "vite",
        "preview": "vite preview",
        "build": "vue-tsc --noEmit && vite build",
        "typecheck": "vue-tsc --noEmit"
    },
    "dependencies": {
        "@bobanm/cardinal": "^0.0.8",
        "@randomness-logger/shared": "workspace:*",
        "ethers": "^6.16.0",
        "vue": "^3.5.35"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^6.0.7",
        "typescript": "^6.0.3",
        "vite": "^8.0.16",
        "vue-tsc": "^3.3.4"
    }
}